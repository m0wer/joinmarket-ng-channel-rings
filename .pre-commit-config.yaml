default_language_version:
  python: python3.14

repos:
  - repo: local
    hooks:
      - id: validate-commit-message
        name: validate commit message
        entry: python scripts/validate_commit_message.py
        language: system
        stages: [commit-msg]
        pass_filenames: true
      - id: complexipy
        name: complexipy
        entry: complexipy . --failed --suggest-refactors
        language: python
        additional_dependencies: [complexipy==5.5.0]
        pass_filenames: false

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.0
    hooks:
      - id: ruff
        args: [--fix, --exit-non-zero-on-fix]
      - id: ruff-format

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
        exclude: ^lnd/patches/.*\.patch$
      - id: end-of-file-fixer
        exclude: ^lnd/patches/.*\.patch$
      - id: check-yaml
        exclude: ^mkdocs\.yml$
      - id: check-yaml
        name: check mkdocs yaml
        args: [--unsafe]
        files: ^mkdocs\.yml$
      - id: check-added-large-files
      - id: check-merge-conflict
      - id: check-toml
      - id: debug-statements

  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.19.1
    hooks:
      - id: mypy
        additional_dependencies: [pydantic>=2.5.0, pydantic-settings>=2.1.0, types-PyYAML>=6.0.0]
        args: [--ignore-missing-imports, --explicit-package-bases, --check-untyped-defs]
